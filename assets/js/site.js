(function () {
  const body = document.body;
  const base = body.dataset.base || "";
  const page = body.dataset.page || "";
  const appSlug = body.dataset.app || "";
  const homeHref = `${base}index.html`;
  const themeKey = "mkvision-theme";

  const navItems = [
    { key: "home", label: "Home", href: homeHref },
    { key: "apps", label: "Apps", href: page === "home" ? "#products" : `${homeHref}#products` },
    { key: "about", label: "About", href: `${base}about/index.html` },
    { key: "support", label: "Support", href: `${base}support/index.html` },
    { key: "privacy", label: "Privacy", href: `${base}privacy/index.html` },
    { key: "terms", label: "Terms", href: `${base}terms/index.html` },
    { key: "contact", label: "Contact", href: `${base}contact/index.html` }
  ];
  const headerRoot = document.querySelector("[data-site-header]");
  const footerRoot = document.querySelector("[data-site-footer]");
  function getTheme(){const saved=localStorage.getItem(themeKey);if(saved==="dark"||saved==="light")return saved;return "dark";}
  function applyTheme(theme){body.dataset.theme=theme;}
  function renderHeader(){if(!headerRoot)return;headerRoot.innerHTML=`<header class="site-header"><div class="container nav-shell"><a class="brand" href="${homeHref}" aria-label="MKVSNX home"><span class="brand-mark">MK</span><span class="brand-text">MKVSNX<small>Apps • Software • AI</small></span></a><div class="nav-actions"><button class="button button-secondary menu-toggle" type="button" aria-expanded="false" aria-controls="site-nav">Menu</button></div><nav class="nav-links" id="site-nav" aria-label="Primary navigation">${navItems.map((item)=>`<a href="${item.href}" ${item.key===page?'aria-current="page"':""}>${item.label}</a>`).join("")}</nav></div></header>`;const toggle=headerRoot.querySelector(".menu-toggle"),nav=headerRoot.querySelector(".nav-links");if(toggle&&nav)toggle.addEventListener("click",()=>{const isOpen=nav.classList.toggle("is-open");toggle.setAttribute("aria-expanded",String(isOpen));});}
  function renderFooter(){if(!footerRoot)return;const year=new Date().getFullYear();const supportHref=appSlug?`${base}${appSlug}/support.html`:`${base}support/index.html`;const privacyHref=appSlug?`${base}${appSlug}/privacy.html`:`${base}privacy/index.html`;const termsHref=appSlug?`${base}${appSlug}/terms.html`:`${base}terms/index.html`;footerRoot.innerHTML=`<footer class="site-footer"><div class="container"><div class="footer-shell footer-grid"><div><strong>MKVSNX</strong><div class="footer-copy">Apps • Software • AI</div><div class="footer-copy">Focused digital products built to evolve.</div></div><div><strong>Explore</strong><div class="footer-stack"><a href="${base}index.html">Home</a><a href="${base}index.html#products">Products</a><a href="${base}about/index.html">About</a><a href="${supportHref}">Support</a></div></div><div><strong>Legal & Contact</strong><div class="footer-stack"><a href="${privacyHref}">Privacy</a><a href="${termsHref}">Terms</a><a href="${base}contact/index.html">Contact</a></div></div><div><strong>Products</strong><div class="footer-stack"><a href="${base}build-book/index.html">Build Book</a><a href="${base}pro-kp-astrology/index.html">Pro KP Astrology</a><span>Reawakening — In Development</span><span>More products — Upcoming</span></div><div class="footer-copy">© ${year} MKVSNX. India.</div></div></div></div></footer>`;}
  function setupReveal(scope){const items=(scope||document).querySelectorAll("[data-reveal]");if(!items.length||!("IntersectionObserver" in window)){items.forEach(i=>i.classList.add("is-visible"));return;}const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target);}})},{threshold:.16});items.forEach(i=>observer.observe(i));}
  function setupRipples(scope){(scope||document).querySelectorAll(".button").forEach(button=>{if(button.dataset.rippleBound==="true")return;button.dataset.rippleBound="true";button.addEventListener("click",event=>{const circle=document.createElement("span");circle.className="ripple";const diameter=Math.max(button.clientWidth,button.clientHeight);circle.style.width=`${diameter}px`;circle.style.height=`${diameter}px`;circle.style.left=`${event.clientX-button.getBoundingClientRect().left-diameter/2}px`;circle.style.top=`${event.clientY-button.getBoundingClientRect().top-diameter/2}px`;button.querySelector(".ripple")?.remove();button.appendChild(circle);});});}
  async function renderAppCards(){const mounts=document.querySelectorAll("[data-app-grid]");if(!mounts.length||!window.MKVSNXApps)return;const apps=await window.MKVSNXApps.load(base);mounts.forEach(mount=>{const mode=mount.dataset.appGrid;const query=(mount.dataset.search||"").trim().toLowerCase();let filtered=mode==="featured"?apps.slice(0,4):apps.slice();if(mode==="coming-soon")filtered=filtered.filter(app=>app.status==="Coming Soon");if(query)filtered=filtered.filter(app=>`${app.name} ${app.category} ${app.description} ${app.features.join(" ")}`.toLowerCase().includes(query));mount.innerHTML=filtered.map(app=>`<article class="app-card" data-reveal><div class="app-icon accent-${app.accent}">${app.icon}</div><div class="pill-row"><span class="pill">${app.category}</span><span class="pill">${app.status}</span></div><h3>${app.name}</h3><p>${app.description}</p><ul class="feature-list">${app.features.slice(0,4).map(feature=>`<li>${feature}</li>`).join("")}</ul><div class="button-row"><a class="button button-primary" href="${base}${app.slug}/${mode==="support"?"support.html":mode==="privacy"?"privacy.html":"index.html"}">${mode==="support"?"Support":mode==="privacy"?"Privacy Policy":"Learn More"}</a><a class="button button-secondary" href="${base}${app.slug}/${mode==="support"?"privacy.html":"support.html"}">${mode==="support"?"Privacy":"Support"}</a></div></article>`).join("");if(!filtered.length)mount.innerHTML=`<article class="info-card" data-reveal><h3>No matching apps</h3><p>Try a different search term or browse the full portfolio.</p></article>`;setupReveal(mount);setupRipples(mount);});}
  function setupSearch(){const input=document.querySelector("[data-app-search]"),mounts=document.querySelectorAll("[data-app-grid-searchable]");if(!input||!mounts.length)return;input.addEventListener("input",()=>{mounts.forEach(m=>m.dataset.search=input.value);renderAppCards();});}
  renderHeader();renderFooter();applyTheme(getTheme());setupReveal();setupRipples();setupSearch();renderAppCards();
  const futureScript=document.createElement("script");futureScript.src=`${base}assets/js/site-future.js`;futureScript.defer=true;document.body.appendChild(futureScript);
})();
